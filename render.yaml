services:
  - type: web
    name: identity-validator
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: WALLET_ADDRESS
        sync: false
